@AGENTS.md

---

# Claude 작업 지침

## 역할

Claude는 군산.com 프로젝트의 개발 파트너입니다.

Claude는 항상 `AGENTS.md`의 모든 규칙을 우선적으로 따릅니다.

군산.com의 큰 브랜드 방향은 다음과 같습니다.

> 군산.com = 군산의 모든 것

다만 현재 개발 단계는 v0.1입니다.
v0.1에서는 군산의 모든 기능을 한 번에 구현하지 않고, 장소 정보 UI와 검증 가능한 정보 구조부터 작게 만듭니다.

---

## 작업 전 반드시 확인할 것

작업을 시작하기 전 반드시 다음을 확인합니다.

- 현재 개발 단계가 v0.1임을 기억합니다.
- v0.1에서 금지된 기능을 구현하지 않습니다.
- 작업 범위가 어떤 GitHub Issue에 해당하는지 확인합니다.
- 작업 범위가 명확하지 않으면 먼저 묻습니다.
- 코드 파일을 수정하기 전 항상 관련 파일을 읽고 시작합니다.
- 요청 범위 밖 파일을 수정하지 않습니다.
- 한 이슈에 여러 작업을 섞지 않습니다.
- 한 커밋에 여러 기능을 섞지 않습니다.

---

## 반드시 따를 문서

작업 전 다음 문서를 읽고 따릅니다.

- `AGENTS.md`
- `docs/WORKFLOW.md`
- `docs/SKILL.md`
- `docs/DESIGN.md`
- `docs/GOAL.md`가 존재하는 경우 `docs/GOAL.md`

각 문서의 역할은 다음과 같습니다.

| 문서               | 역할                                     |
| ------------------ | ---------------------------------------- |
| `AGENTS.md`        | 프로젝트 전체 작업 규칙                  |
| `docs/WORKFLOW.md` | 이슈·커밋·푸시·브랜치·AI 권한 기준       |
| `docs/SKILL.md`    | 타입 정의, 더미 데이터, 페이지 구현 규칙 |
| `docs/DESIGN.md`   | 색상, 레이아웃, 컴포넌트 디자인 규칙     |
| `docs/GOAL.md`     | 군산.com의 브랜드 목표와 제품 방향       |

`docs/GOAL.md`가 아직 없다면 `AGENTS.md`의 프로젝트 개요와 제품 방향을 기준으로 작업합니다.

---

## 응답 언어

- 한국어로 응답합니다.
- 작업 보고도 한국어로 작성합니다.
- 커밋 메시지도 한국어로 제안합니다.

---

## v0.1에서 절대 구현하지 말 것

현재 v0.1에서는 다음 기능을 선제적으로 구현하지 않습니다.

- Supabase 연결
- DB 저장
- 지도 기능
- 사진 업로드
- 관리자 페이지
- 로그인
- 결제
- 광고 상품
- 사장님 대시보드
- 자유게시판
- 행사/축제 캘린더 실제 구현
- 외부 데이터 크롤링
- 외부 리뷰/사진/본문 복사

위 기능은 장기적으로 가능하더라도, 현재 이슈에서 사용자가 명시하지 않았다면 구현하지 않습니다.

---

## 코드 작성 규칙

- TypeScript `any`를 사용하지 않습니다.
- 타입이 불명확하면 `unknown`을 사용하고 narrowing합니다.
- 타입은 가능한 한 `types/` 폴더에 분리합니다.
- 컴포넌트 내부에 임시 타입을 남발하지 않습니다.
- 업체 제공 정보와 방문자 확인 정보를 하나의 타입이나 컴포넌트 안에서 섞지 않습니다.
- 확인되지 않은 정보는 “확인 필요”로 표시합니다.
- 외부 사이트의 사진, 리뷰, 본문은 무단 복사하지 않습니다.

---

## 필드명 규칙

`docs/SKILL.md` 기준을 따릅니다.

- 동네/지역 필드는 `area`를 사용합니다.
- `neighborhood` 필드는 사용하지 않습니다.
- 검증 상태 목록은 `statuses`를 사용합니다.
- `verificationStatuses` 필드는 사용하지 않습니다.
- 카테고리 표기는 `/`를 사용합니다.
  - `네일/뷰티`
  - `병원/약국`

- 카테고리 표기에 `·`를 사용하지 않습니다.

---

## 이슈커밋푸시 자동 진행

사용자가 다음과 같이 명시하면, 해당 작업에 한해 이슈 생성 → 검증 → 커밋 → 푸시를 순서대로 실행할 수 있습니다.

- `이슈커밋푸시 진행해`
- `이 작업 이슈 만들고 커밋하고 푸시까지 해줘`
- `issue commit push까지 진행해`

상세 순서, 중단 조건, 권한 기준은 `docs/WORKFLOW.md`의 **이슈커밋푸시 자동 진행 규칙**을 따릅니다.

단, 사용자가 명시하지 않으면 commit/push를 실행하지 않습니다.

---

## commit/push 권한 기준

Claude는 사용자의 명시적 지시 없이 다음을 실행하지 않습니다.

- `git commit`
- `git push`
- `git push --force`
- 배포 설정 변경
- 환경변수 변경
- 외부 서비스 연결
- Supabase 프로젝트 생성
- 도메인 설정 변경

사용자가 `이슈커밋푸시 진행해`라고 명시한 경우에만 해당 작업 범위 안에서 commit/push까지 진행할 수 있습니다.

---

## 작업 완료 보고 형식

작업이 끝나면 반드시 아래 형식으로 보고합니다.

```txt
변경 파일
- ...

주요 변경
- ...

확인
- ...

TODO
- ...

제안 커밋 메시지
- 작업 내용 #이슈번호
```

코드 변경이 있는 경우 확인 항목에는 다음을 포함합니다.

```txt
확인
- npm run lint
- npm run build
```

문서만 수정한 경우에는 다음처럼 보고합니다.

```txt
확인
- 문서 작업이므로 npm run build는 생략 가능
- git diff로 변경 범위 확인 필요
```

---

## 절대 하지 말 것

- TypeScript `any` 사용
- 업체 제공 정보와 방문자 확인 정보를 하나의 컴포넌트나 타입에 혼합
- v0.1 범위 밖의 기능 선제 구현
- 외부 사이트 콘텐츠 무단 복사
- 외부 데이터 무단 크롤링
- 자유게시판 선제 구현
- 행사/축제 캘린더 선제 구현
- 사용자가 명시하지 않은 기능 추가
- 사용자 명시적 지시 없이 commit 실행
- 사용자 명시적 지시 없이 push 실행
- force push
- 작업 범위 밖의 파일 수정
- 이슈 없이 큰 기능 구현
- 한 커밋에 여러 기능 섞기
- 실패한 build 상태로 main push
- `neighborhood` 필드 사용
- `verificationStatuses` 필드 사용
- 카테고리 표기에 `·` 사용
